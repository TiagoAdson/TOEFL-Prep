# Secrets — never commit
.env
.env.local
.env.production

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Capacitor native platforms (regenerate with: npx cap add android)
android/
ios/

# Android release signing — NEVER commit (loses ability to update the app on Play Store if leaked)
*.jks
*.keystore
keystore.properties

# Supabase CLI local cache
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*
